name: Build and Release EXE

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write 

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pyinstaller
        if (Test-Path "requirements.txt") { pip install -r requirements.txt }

    - name: Build with PyInstaller
      run: |
        pyinstaller --onefile --windowed --add-data "data.json;." --add-data "assets;assets" main.py
        # --add-data "data.json;." puts the json at the root of the app
        # --add-data "assets;assets" bundles your whole assets folder recursively

    - name: Upload to GitHub Releases
      uses: softprops/action-gh-release@v2
      with:
        files: dist/*.exe
        generate_release_notes: true